import path from 'path';

export const IMAGE_LOCAL_PATH = path.join('public', 'images');

export const SUPPORTED_IMAEG_EXTENSIONS = ['image/png', 'image/jpg', 'image/jpeg'];

export const MAX_FILE_SIZE = 3 * 1024 * 1024;
